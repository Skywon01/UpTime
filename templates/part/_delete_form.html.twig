<form method="post" action="{{ path('app_part_delete', {'id': part.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette pièce?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ part.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
