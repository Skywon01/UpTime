<form method="post" action="{{ path('app_machine_delete', {'id': machine.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer la machine?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ machine.id) }}">
    <button class="btn btn-warning mt-5">Supprimer</button>
</form>
