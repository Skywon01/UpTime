<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}UpTime - GMAO{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

    <style>
        :root {
            --sidebar-width: 240px;
            --primary-color: #0d6efd;
            --dark-bg: #1a1d20;
        }

        body { background-color: #f8f9fc; min-height: 100vh; }

        #wrapper { display: flex; width: 100%; }

        /* Sidebar Style */
        #sidebar {
            width: var(--sidebar-width);
            min-height: 100vh;
            background: var(--dark-bg);
            position: fixed;
            transition: all 0.3s;
            z-index: 1000;
        }

        .sidebar-header {
            padding: 1.5rem;
            background: #141618;
            font-size: 1.25rem;
            font-weight: bold;
            color: white;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }

        .nav-pills .nav-link {
            color: rgba(255,255,255,0.7);
            padding: 0.8rem 1.5rem;
            border-radius: 0;
            margin: 0.2rem 0;
            display: flex;
            align-items: center;
        }

        .nav-pills .nav-link i { font-size: 1.1rem; width: 25px; }

        .nav-pills .nav-link:hover {
            background: rgba(255,255,255,0.1);
            color: white;
        }

        .nav-pills .nav-link.active {
            background: var(--primary-color);
            color: white;
            border-right: 4px solid white;
        }

        /* Content Area */
        #content {
            width: 100%;
            margin-left: var(--sidebar-width);
            padding: 2rem;
        }

        /* Responsivité mobile légère */
        @media (max-width: 768px) {
            #sidebar { margin-left: calc(-1 * var(--sidebar-width)); }
            #content { margin-left: 0; }
        }
    </style>

    {% block stylesheets %}{% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>

<body>
<div id="wrapper">
    <nav id="sidebar">
        <div class="sidebar-header">
            <span class="text-primary">⚓</span> UpTime
        </div>

        <div class="p-3">
            <ul class="nav nav-pills flex-column">
                <li class="nav-item">
                    <a href="{{ path('app_dashboard_index') }}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_dashboard' ? 'active' }}">
                        <i class="bi bi-speedometer2 me-2"></i> Dashboard
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_company_index') }}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_company' ? 'active' }}">
                        <i class="bi bi-building me-2"></i> Entreprises
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_machine_index') }}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_machine' ? 'active' }}">
                        <i class="bi bi-cpu me-2"></i> Machines
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_intervention_index') }}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_intervention' ? 'active' }}">
                        <i class="bi bi-tools me-2"></i> Interventions
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_part_index') }}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_part' ? 'active' }}">
                        <i class="bi bi-box-seam me-2"></i> Pièces
                    </a>
                </li>
            </ul>
        </div>
    </nav>

    <div id="content">
        {% block body %}{% endblock %}
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
