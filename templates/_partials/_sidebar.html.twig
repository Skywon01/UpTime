<nav id="sidebar">
    <div class="sidebar-header d-flex justify-content-between align-items-center">
        <span><span class="text-primary">⚓</span> UpTime</span>
        <button type="button" id="sidebarCollapseDesktop" class="btn btn-link text-white d-none d-md-block p-0">
            <i class="bi bi-arrow-left-square-fill fs-4"></i>
        </button>
        <button type="button" id="sidebarCloseMobile" class="btn btn-link text-white d-md-none p-0">
            <i class="bi bi-x-lg fs-4"></i>
        </button>
    </div>

    <div class="p-3">
        <ul class="nav nav-pills flex-column">
            {% set current_route = app.request.attributes.get('_route') %}
            <li class="nav-item">
                <a href="{{ path('app_dashboard_index') }}" class="nav-link {{ current_route starts with 'app_dashboard' ? 'active' }}">
                    <i class="bi bi-speedometer2"></i> <span>Dashboard</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_machine_index') }}" class="nav-link {{ current_route starts with 'app_machine' ? 'active' }}">
                    <i class="bi bi-cpu"></i> <span>Machines</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_intervention_index') }}" class="nav-link {{ current_route starts with 'app_intervention' ? 'active' }}">
                    <i class="bi bi-tools"></i> <span>Interventions</span>
                </a>
            </li>
        </ul>
    </div>
</nav>
