<nav id="sidebar">
    <div class="sidebar-header d-flex justify-content-between align-items-center">
        <span><span class="text-primary">⚓</span> UpTime</span>
        <button type="button" id="sidebarCollapseDesktop" class="btn btn-sm btn-light border shadow-sm d-none d-md-flex align-items-center justify-content-center p-0"
                style="width: 24px; height: 24px; border-radius: 50%; position: absolute; right: -12px; top: 20px; z-index: 1060;">
            <i class="bi bi-chevron-left small text-dark"></i>
        </button>
        <button type="button" id="sidebarCloseMobile" class="btn btn-link text-white d-md-none p-0">
            <i class="bi bi-x-lg fs-4"></i>
        </button>
    </div>

    <div class="p-3">
        <ul class="nav nav-pills flex-column">
            {% set current_route = app.request.attributes.get('_route') %}

            {# --- SECTION DASHBOARD --- #}
            <li class="nav-item">
                <a href="{{ path('app_dashboard_index') }}" class="nav-link {{ current_route == 'app_dashboard_index' ? 'active' }}">
                    <i class="bi bi-speedometer2"></i> <span>Dashboard</span>
                </a>
            </li>

            {# --- SECTION MAINTENANCE --- #}
            <div class="text-muted small fw-bold text-uppercase px-3 mt-4 mb-2 opacity-50">Maintenance</div>
            <li class="nav-item">
                <a href="{{ path('app_machine_index') }}" class="nav-link {{ current_route starts with 'app_machine' ? 'active' }}">
                    <i class="bi bi-cpu"></i> <span>Machines</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_intervention_index') }}" class="nav-link {{ current_route starts with 'app_intervention' ? 'active' }}">
                    <i class="bi bi-tools"></i> <span>Interventions</span>
                </a>
            </li>

            {# --- SECTION LOGISTIQUE --- #}
            <div class="text-muted small fw-bold text-uppercase px-3 mt-4 mb-2 opacity-50">Gestion & Stocks</div>
            <li class="nav-item">
                <a href="{{ path('app_part_index') }}" class="nav-link {{ current_route starts with 'app_part' ? 'active' }}">
                    <i class="bi bi-box-seam"></i> <span>Pièces détachées</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_supplier_index') }}" class="nav-link {{ current_route starts with 'app_supplier' ? 'active' }}">
                    <i class="bi bi-truck"></i> <span>Fournisseurs</span>
                </a>
            </li>

            {# --- SECTION PARAMÈTRES (NOUVEAU) --- #}
            <div class="text-muted small fw-bold text-uppercase px-3 mt-4 mb-2 opacity-50">Configuration</div>
            <li class="nav-item">
                <a href="{{ path('app_company_index') }}" class="nav-link {{ current_route starts with 'app_company' ? 'active' }}">
                    <i class="bi bi-building-gear"></i> <span>Mon Entreprise</span>
                </a>
            </li>
        </ul>
    </div>
</nav>
